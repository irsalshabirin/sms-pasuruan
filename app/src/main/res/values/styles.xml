<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Button. -->
    <style name="AppButton" parent="@style/Widget.AppCompat.Button">
        <!--<item name="android:paddingLeft">@dimen/button_horizontal_inset</item>-->
        <!--<item name="android:paddingRight">@dimen/button_horizontal_inset</item>-->
        <!--<item name="android:minWidth">@dimen/button_min_width</item>-->
        <item name="android:textColor">@android:color/white</item>
    </style>


    <style name="AppButton.ColorPrimary">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:backgroundTint">@color/colorPrimary</item>

        <item name="backgroundTint">@color/colorPrimary</item>
    </style>

    <style name="AppButton.ColorAccent">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:backgroundTint">@color/colorAccent</item>

        <item name="backgroundTint">@color/colorAccent</item>
    </style>

    <style name="AppButton.Red">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:backgroundTint">@android:color/holo_red_dark</item>

        <item name="backgroundTint">@android:color/holo_red_dark</item>
    </style>

    <style name="AppButton.White">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:backgroundTint">@android:color/white</item>

        <item name="backgroundTint">@android:color/white</item>
    </style>
</resources>
